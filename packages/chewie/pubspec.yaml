name: chewie
description: A lightweight video player wrapper for Flutter (minimal fork without controls)
version: 1.13.0

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.10.0

flutter:
  uses-material-design: true
